Scaffold(
    appBar: AppBar(
      title: const Text('Home'),
    ),
    body: const Center(
      child: Text('Nuestro primer texto centrado'),


       @override
  // ignore: dead_code
  Widget build(BuildContext context) {
    return const Center(
      child: Text('vista separada desde Home Screen')
      );